declare type FetchFunc<Result, Input> = (input: Input) => Promise<Result>;
/**
 * fetch store
 *
 * `get` will throw a promise when a result is not ready.
 * `prefetch` will start fetching.
 * `evict` will remove a result.
 * If `input` is an object, a result will be stored in WeakMap.
 * Othrewise, a result will be stored in Map.
 */
export declare type FetchStore<Result, Input> = {
    get: (input: Input) => Result;
    prefetch: (input: Input) => void;
    evict: (input: Input) => void;
};
/**
 * create fetch store
 *
 * @example
 * import { createFetchStore } from 'react-suspense-fetch';
 *
 * const fetchFunc = async (userId) => (await fetch(`https://reqres.in/api/users/${userId}?delay=3`)).json();
 * const store = createFetchStore(fetchFunc);
 * store.prefetch('1');
 */
export declare function createFetchStore<Result, Input>(fetchFunc: FetchFunc<Result, Input>, preloaded?: Iterable<readonly [Input, Result]>): FetchStore<Result, Input>;
export {};
